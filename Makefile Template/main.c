#include <stdio.h>

int main(void)
{
    int width=0,height=0,perimeter=0,area=0;
        
        width=5;
        height=10;
        area = width * height;
        perimeter = 2*(width + height);
        printf("Area of the rectangle is : %d\n",area);
        printf("Perimeter of the rectangle is :%d\n",perimeter);

        
}
